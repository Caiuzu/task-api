import { GenericExceptionsFilter } from './generic-exceptions.filter';

describe('GenericExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new GenericExceptionsFilter()).toBeDefined();
  });
});
